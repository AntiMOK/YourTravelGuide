{
  "name": "your-local-guide-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "firebase emulators:start",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^12.3.0"
  },
  "devDependencies": {
    "vite": "^7.1.9"
  }
}
